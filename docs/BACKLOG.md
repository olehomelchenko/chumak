## Backlog for the project

This document contains a list of features and tasks that need to be completed to enhance the project functionality and improve user experience.

### Enhancements

- Icons in the .panel-section for upload and import should be more prominent and visible, possibly located not alongside but below the text. They should also be more distinct to quickly suggest the place where new data can be added
  - When no data is selected, the "Get Started with Chumak" could have the buttons that duplicate the import options on the sidebar panel.

- the Floating Type menu .type-menu should contain the same set of icons as the .type-indicator icons in the table header. The same should be done across other places where the type indicators are used, with duplication of the text labels when space allows for that.

- remove the "new model" under the list of models for each data source in the sidebar.

### Bugs
