/**
 * Aggregate dialog states — window only.
 * (aggregate, pivot, fold migrated to useDialogState)
 */

export {
  windowState,
  resetWindowState,
  type WindowFunction,
  type OrderByItem,
} from './window-state';
