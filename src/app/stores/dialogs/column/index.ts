/**
 * Column dialog states
 *
 * spread, unroll, merge, column-editor, split, dedupe: migrated to useDialogState
 * Only type-conversion remains (non-transform dialog).
 */

export { typeConversionState, resetTypeConversionState } from './type-conversion-state';
